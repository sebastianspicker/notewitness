[project]
name = "notewitness"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
[tool.pytest.ini_options]
addopts = "-q"
# forced-evidence-3:exploration
# forced-workbench-4:core-build-out
# forced-evidence-5:maintenance
# forced-evidence-6:maintenance
